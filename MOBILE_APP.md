# AvenueBoard Mobile App Shell

Phase 1 mobile/PWA route: `/mobile`

Future Capacitor target route: `/mobile`

The mobile shell is intentionally separate from the public website and desktop dashboards. Keep native app wrapper work pointed at `/mobile` so the existing web experience remains unchanged.
