# AvenueBoard Legacy Root Landing Page

Archived on July 22, 2026.

This folder preserves the previous AvenueBoard root landing page that was active at `/`.
It was archived rather than deleted so the implementation remains recoverable from the
repository.

The active replacement route is `/`, which now uses the latest landing-page implementation.

Do not import this archived page from production code. Shared assets such as `/logo.png`,
`/landlord.png`, and `/tenantpreview.png` remain in `public/` because they are used by
active public routes and are not specific to this archive.
