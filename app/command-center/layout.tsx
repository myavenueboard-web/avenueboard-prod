export const metadata = {
  title: "Command Center | AvenueBoard",
  robots: {
    index: false,
    follow: false,
  },
};

export default function CommandCenterRootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
